<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title><%= pageTitle %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/css/form.css">
</head>
<body>
  <header class="topbar">
    <div class="container topbar-inner">
      <div class="logo">Lab 7 • Ticket Order App</div>
      <nav class="nav">
        <a href="/" class="<%= currentPage === 'form' ? 'active' : '' %>">Order Form</a>
        <a href="/submissions" class="<%= currentPage === 'submissions' ? 'active' : '' %>">Submissions</a>
      </nav>
    </div>
  </header>
  <main class="container">
